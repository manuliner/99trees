{
  "name": "99trees",
  "private": true,
  "scripts": {
    "dev": "pnpm -C web dev",
    "build": "pnpm -C web build",
    "generate": "pnpm -C web generate",
    "preview": "pnpm -C web preview",
    "postinstall": "pnpm -C web postinstall",
    "typecheck": "pnpm -C web typecheck",
    "db:generate": "pnpm -C web db:generate",
    "db:migrate": "pnpm -C web db:migrate",
    "db:seed": "pnpm -C web db:seed",
    "db:reset": "pnpm -C web db:reset"
  }
}
