# docs-shared (template)

Copied into **each** canonical skill during scaffold. Holds shared `scripts/` and `reference/` used by verification and authoring helpers.

Standalone `SKILL.md` defines maintenance of these assets; sibling skills specialize workflows.
