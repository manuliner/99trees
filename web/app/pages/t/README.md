# Team QR deep link

**Purpose:** Team rejoin QR resolves edition and sends crew to login with prefilled token.

- **[slug].vue** — resolves `/t/:slug?t=…` via `/api/public/team-qr` → `/:edition/crew/login`
