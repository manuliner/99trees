# App config

**Purpose:** Client-side feature toggles not tied to a single component.

- **goal-celebration.ts** — `GOAL_CELEBRATION_RAIN_STYLE` (`classic` | `wanderkarten`) for goal-reached animation
